import Testing
import FluidAudio

@Test func fluidAudioImportResolves() {
    // Smoke test: if this file compiles, FluidAudio is correctly linked.
    // No runtime assertions needed — compilation IS the test.
    #expect(true)
}
