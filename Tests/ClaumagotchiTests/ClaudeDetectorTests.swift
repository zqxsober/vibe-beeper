import XCTest

/// Tests for ClaudeDetector — stub only.
/// Real tests are added in Plan 01 after ClaudeDetector.swift is created.
/// These tests exercise ClaudeDetector in-process by spawning the built CLI
/// or by referencing known filesystem paths (no @testable import needed).
final class ClaudeDetectorTests: XCTestCase {

    func testClaudeDetectorStub() throws {
        // Placeholder — replaced in Plan 01
        // Expected behavior: ClaudeDetector.isInstalled returns true when
        // ~/.local/bin/claude or a Homebrew path exists.
        XCTAssertTrue(true, "Stub: replaced in Plan 01")
    }

    func testClaudeDirExistsStub() throws {
        // Placeholder — replaced in Plan 01
        // Expected behavior: ClaudeDetector.claudeDirExists returns true when
        // ~/.claude/ directory exists on disk.
        XCTAssertTrue(true, "Stub: replaced in Plan 01")
    }
}
